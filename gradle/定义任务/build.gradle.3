//description 关键字 任务添加描述
task copy(type: Copy) {
   description 'Copies the resource directory to the target directory.'
   from 'resources'
   into 'target'
   include('**/*.txt', '**/*.xml', '**/*.properties','**/*.gradle')
   println("description applied")
}